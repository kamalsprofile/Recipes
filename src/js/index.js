import str from './views/searchView';
import { add, multiply, ID } from './modules/Search'
console.log(`added numbers ${add(ID, 5)} multiplied numbers are ${multiply(5, ID)} and the string from SearchView is ${str}`)